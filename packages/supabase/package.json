{
  "name": "@workspace/supabase",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "gen": "supabase gen types --local --lang=typescript --schema=public,storage > ./types.ts",
    "lint": "eslint . --max-warnings 0"
  },
  "dependencies": {
  },
  "devDependencies": {
  },
  "exports": {
    "./*": "./*"
  }
}
